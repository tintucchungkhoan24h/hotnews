name: Update Sitemap
permissions:
  contents: write
on:
  schedule:
    # Run daily at 00:00 UTC (7:00 AM Vietnam time)
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  update-sitemap:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5  # <--- Upgraded to v5 to clear the warning
        with:
          persist-credentials: true
          
      - name: Setup Node.js
        uses: actions/setup-node@v5  # <--- Upgraded to v5 to clear the warning
        with:
          node-version: '24'
          
      - name: Generate digest pages and update sitemap
        run: |
          node scripts/generate-digest.mjs
          node scripts/generate-digest-macro.mjs
        
      - name: Commit and push changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add sitemap.xml diem-tin-chung-khoan/ diem-tin-vi-mo/
          git diff --staged --quiet || git commit -m "Auto-update sitemap and digest pages [skip ci]"
          git push "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git" HEAD:${{ github.ref_name }}